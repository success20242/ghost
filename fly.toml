app = "trendifytube-blog"  # Your Fly app name
region = "cdg"

kill_signal = "SIGINT"
kill_timeout = 5

[build]
  image = "ghost:latest"

[env]
  url = "https://trendifytube-blog.fly.dev"

[mounts]
  source = "ghost_data"
  destination = "/var/lib/ghost/content"
